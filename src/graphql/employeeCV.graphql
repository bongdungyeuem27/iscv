query GetDefaultCV($employeeId: Int!) {
  defaultCV(employeeId: $employeeId) {
    id
    user
    name
    phone
    professional
    email
    github
    linkedin
    sourceImage
    skills {
      id
      employeeId
      title
      level
    }
  }
}
